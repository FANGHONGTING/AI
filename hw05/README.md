# 实验五：CNN手写数字识别

> 本文件即为实验报告

## 目录结构
hw05/
├── simple_cnn.py # 任务一：极简CNN代码（来源：公众号文章）
├── lenet5.py # 任务二：LeNet-5实现
├── requirements.txt # 依赖包列表
├── debug_notes.md # 调试记录
└── README.md # 实验报告（本文件）

## 一键训练/评估两个模型

运行以下命令即可分别训练和评估两个模型：

- **极简CNN（任务一）**：`python simple_cnn.py`
- **LeNet-5（任务二）**：`python lenet5.py`

两个脚本会自动：
- 下载MNIST数据集（首次运行）
- 训练5个epoch
- 输出每个epoch的损失和准确率
- 输出测试集准确率
- 保存模型为 `.pth` 文件

> 注：MNIST数据集会自动下载到 `./data/` 目录，若下载失败请手动下载并放置于该目录。

## 环境配置与运行命令

### Python版本
- Python 3.8 或更高版本（推荐 Python 3.10）

### 安装依赖

```bash
pip install -r requirements.txt
或使用清华镜像源（国内用户推荐）：
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

## 任务一：极简CNN

### 文章要点概括

这篇文章讲解了如何使用PyTorch构建一个极简的卷积神经网络来识别MNIST手写数字。

**核心知识点：**

1. CNN的四个核心组件：卷积层（提取特征）、激活函数（引入非线性）、池化层（降采样减少计算量）、全连接层（分类）

2. SimpleCNN模型结构：
   - 输入：28×28灰度图像
   - 卷积层：1通道→16通道，3×3卷积核，padding=1（保持尺寸不变）
   - ReLU激活
   - 最大池化层：2×2窗口，步长2（28×28→14×14）
   - 全连接层：3136维 → 10维（0-9分类）

3. 训练流程：
   - 数据加载：从torchvision下载MNIST，归一化处理
   - 前向传播：图像→卷积→ReLU→池化→展平→全连接→输出
   - 损失计算：交叉熵损失函数
   - 反向传播：计算梯度
   - 参数更新：Adam优化器，学习率0.001
   - 训练5个epoch，批量大小64

4. 模型参数：总共31,530个可训练参数
### 运行结果

**配置项汇总：**

| 配置项 | 值 |
|--------|-----|
| 训练轮数 | 5 |
| 批量大小 | 64 |
| 优化器 | Adam |
| 学习率 | 0.001 |
| 使用设备 | CPU |
| 训练耗时（5轮） | 约 2 分 18 秒 |
| 测试准确率 | 98.04% |
| 测试损失 | 0.0592 |

**训练过程（每个Epoch的表现）：**

| Epoch | 损失 | 准确率 |
|-------|------|--------|
| 1 | 0.2324 | 93.14% |
| 2 | 0.0891 | 97.37% |
| 3 | 0.0652 | 98.10% |
| 4 | 0.0546 | 98.35% |
| 5 | 0.0464 | 98.61% |

## 任务二：LeNet-5

### 网络结构

LeNet-5是由Yann LeCun提出的经典卷积神经网络，最初用于手写数字识别。针对MNIST数据集（28×28），我实现了以下结构：

| 层 | 操作 | 输入尺寸 | 输出尺寸 | 参数量 |
|----|------|----------|----------|--------|
| 输入 | - | 28×28×1 | 28×28×1 | 0 |
| C1 | 卷积5×5, 6个, padding=2 | 28×28×1 | 28×28×6 | 156 |
| S2 | 最大池化2×2, 步长2 | 28×28×6 | 14×14×6 | 0 |
| C3 | 卷积5×5, 16个 | 14×14×6 | 10×10×16 | 2,416 |
| S4 | 最大池化2×2, 步长2 | 10×10×16 | 5×5×16 | 0 |
| C5 | 卷积5×5, 120个 | 5×5×16 | 1×1×120 | 48,120 |
| F6 | 全连接84 | 120 | 84 | 10,164 |
| 输出 | 全连接10 | 84 | 10 | 850 |
| **总计** | - | - | - | **61,706** |

### 训练配置

| 配置项 | 值 |
|--------|-----|
| 训练轮数 | 5 |
| 批量大小 | 64 |
| 优化器 | Adam |
| 学习率 | 0.001 |
| 使用设备 | CPU |
| 训练耗时（5轮） | 约 2 分 55 秒 |

### 运行结果

**训练过程（每个Epoch的表现）：**

| Epoch | 损失 | 准确率 |
|-------|------|--------|
| 1 | 0.2200 | 93.22% |
| 2 | 0.0636 | 98.06% |
| 3 | 0.0477 | 98.51% |
| 4 | 0.0386 | 98.80% |
| 5 | 0.0299 | 99.04% |

**测试结果汇总：**

| 指标 | 值 |
|------|-----|
| 测试损失 | 0.0350 |
| 测试准确率 | 98.92% |

---

## 任务三：模型对比

| 对比项 | 极简CNN (SimpleCNN) | LeNet-5 |
|--------|---------------------|---------|
| 测试准确率 | 98.04% | 98.92% |
| 参数量 | 31,530 | 61,706 |
| 训练耗时（5轮） | 约 2 分 18 秒 | 约 2 分 55 秒 |
| 卷积层数 | 1 | 2 |
| 池化层数 | 1 | 2 |
| 全连接层数 | 1 | 3 |

### 分析

1. **准确率对比**：LeNet-5的准确率（98.92%）高于极简CNN（98.04%），提升了约0.9%。这是因为LeNet-5拥有更深的网络结构（2个卷积层+2个池化层），能够提取更丰富的层次化特征，从而获得更好的分类效果。

2. **参数量对比**：LeNet-5的参数量（61,706）约为极简CNN（31,530）的2倍。LeNet-5的全连接层（C5、F6、输出层）贡献了大部分参数增量，这使得模型具有更强的表达能力，但也需要更多的计算资源和训练时间。

3. **训练耗时对比**：LeNet-5的训练耗时（约2分55秒）比极简CNN（约2分18秒）多约37秒，与参数量的增加成正比。

4. **结论**：LeNet-5通过增加网络深度和宽度，在参数量翻倍、训练时间增加约37秒的情况下换取了约0.9%的准确率提升。对于MNIST这种相对简单的任务，极简CNN已经能达到98%以上的准确率，性价比更高；而LeNet-5的结构设计为更复杂的图像识别任务奠定了基础。

---

## 调试记录

### 错误1：pip安装网络超时

- **现象**：执行 `pip install torch torchvision matplotlib numpy` 时报错，显示网络连接问题
- **原因分析**：默认的PyPI源在国外，网络不稳定
- **修改点**：使用清华镜像源 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 成功安装

### 错误2：ModuleNotFoundError: No module named 'torch'

- **现象**：在普通CMD或Anaconda Prompt中运行 `python simple_cnn.py` 提示找不到torch
- **原因分析**：电脑有多个Python环境（Anaconda、Miniconda、系统Python），torch只安装在了其中一个
- **修改点**：使用完整路径 `C:\Anaconda\python.exe simple_cnn.py` 运行，确保使用正确的Python环境

### 总结
两个模型均成功运行，未遇到代码逻辑错误。环境配置问题已解决。

---

## 代码来源

- 任务一（极简CNN）代码基于微信公众号文章《计算机视觉》第10篇实现
- 原文链接：https://mp.weixin.qq.com/s/iBNvhk-uAeAfTuanxiLs9Q

## 参考文献

1. 微信公众号文章《计算机视觉》第10篇：极简卷积神经网络CNN识别手写数字
2. LeNet-5 原始论文：LeCun, Y., et al. (1998). Gradient-based learning applied to document recognition.
